\section{Benchmark Protocol and Release}


Each sub-type ships three things (Fig. \ref{fig:pipeline}): \textbf{(1)} the success-conditioned unsafe rate of an unmodified policy with its interval; \textbf{(2)} fixability ablations where the rate can move; \textbf{(3)} a feasibility witness, so the rate is the policy's, not the scene's. A general guard is left to future work; the external layers here are instruments. \textbf{Release.} Scenes, recorders, scripts and every per-episode log are released anonymously; a policy is a server swap, a task a new scene family. Thresholds: Appendix H.

