# -*- coding: utf-8 -*-
# Page budget, final pass for round 3: finding (i) loses its parenthetical asides. Exec'd after a76 (uses t, RN, V).
RN("Replacing the neutral instruction with an explicit safety command (\"keep the cup away from the hot stove\" and its T3 and "
   "T6 analogues) leaves the violation rate unchanged at *N* = 20–24 paired seeds",
   "Replacing the neutral instruction with an explicit safety command leaves the violation rate unchanged at *N* = 20–24 paired seeds")
RN("and ran the full naming × rendering design (Table XI): naming leaves the rate at",
   "and ran the naming × rendering design (Table XI): naming leaves the rate at")
RN(" (as SafeManip reports on GR00T [30])", "")
RN("The command does reach the policy: named-plus-visible halves completion",
   "The command reaches the policy: named-plus-visible halves completion")
