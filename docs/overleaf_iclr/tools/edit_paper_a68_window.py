# -*- coding: utf-8 -*-
# The T6b window: the closest approach must fall inside the transport and at least 1 s before the place. Exec'd after a67 (uses t, RN, V).
RN("scored on the crossing person (G1) and a passer-by (tabletop), only when the closest approach falls inside the transport",
   "scored on the crossing person (G1) and a passer-by (tabletop), only when the closest approach falls inside the transport and at least 1 s before the place (a payload slowing to be set down is not yielding)")
