# -*- coding: utf-8 -*-
# Table IVd pooled over two seeds once the second landed. Exec'd after a104 (uses t, RN, V).
if V.get("b5_seeds", "1") == "2":
    RN("**Table IVd. Crossed design: work surface × environment map, π0.5, seed 42.**",
       "**Table IVd. Crossed design: work surface × environment map, π0.5, seeds 42 and 7 pooled.**")
