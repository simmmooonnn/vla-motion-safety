# -*- coding: utf-8 -*-
# Page budget: two clauses in §8, and the round-3 disclosures folded into the same sentences rather than added to them.
# Exec'd after a77 (uses t, RN, V).
RN("T2 has **no witness**; T3 and T4 have one on the tabletop only (the scripted carry), T1 on the G1 only.",
   "T2 has **no witness**; T3 has one on the tabletop (the scripted carry, whose payload is held kinematically, so it "
   "witnesses geometry only and leaves **T4 unattributed**), T1 on the G1 only.")
RN("the G1 person cell's payload is labelled, not physically, hazardous, and **the people have no state**: every proxy is "
   "static or kinematic and never reacts, so every contact rate is an exposure rate, not a harm rate,",
   "the G1 person cell's payload is labelled, not physically, hazardous, and **the people have no state**: every proxy is a "
   "capsule that is static or kinematic, rendered as such to the policy, and reacts only by retreating on contact in one "
   "variant, so every contact rate is an exposure rate, not a harm rate,")
RN("On GR00T two proxies are weak — a box's long axis for a hazardous axis (T3), a rigid box that cannot spill (T4) — which "
   "the tabletop's scissors and mug replace; the people are capsules and the link metric uses link origins.",
   "On GR00T two proxies are weak — a box's long axis for a hazardous axis (T3), a rigid box that cannot spill (T4) — which "
   "the tabletop's scissors and mug replace; the link metric uses link origins.")
