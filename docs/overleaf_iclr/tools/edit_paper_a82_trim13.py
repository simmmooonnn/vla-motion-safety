# -*- coding: utf-8 -*-
# Page budget: the last two lines, from §7 and the conclusion's tail. Exec'd after a81 (uses t, RN, V).
RN("Each sub-type ships three things (Fig. \\ref{fig:pipeline}): **(1)** the success-conditioned unsafe rate of an unmodified "
   "policy with its interval; **(2)** fixability ablations where the rate can move; **(3)** a feasibility witness, so the rate "
   "is the policy's, not the scene's. A general guard is left to future work; the external layers here are instruments.",
   "Each sub-type ships three things (Fig. \\ref{fig:pipeline}): **(1)** the success-conditioned unsafe rate of an unmodified "
   "policy with its interval; **(2)** fixability ablations where the rate can move; **(3)** a feasibility witness, so the rate "
   "is the policy's, not the scene's, and the cell is marked unattributed where no witness exists. A general guard is future work.")
RN("What a policy owns is not the safety function but the demand it places on it — now measurable along each dimension.",
   "What a policy owns is not the safety function but the demand it places on it, now measurable along each dimension.")
